server:
  port: ${PORT:5001}
spring:
   application:
      name: sampleConfig
   profiles:
      active: local
   cloud:
      config:
         server:
            git:
               uri: https://github.com/smluv82/sampleConfigRepository